### Android Build

increment versionCode in app/build.gradle file

On the terminal inside the android directory:

```bash
fastlane build_android  username:<user> version_name:<version> track:internal version_code:<version_code>
```