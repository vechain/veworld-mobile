buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 35
        ndkVersion = "26.1.10909125"
        // Expo SDK 52 / expo-modules-core expect Kotlin 1.9.24/1.9.25; align to 1.9.24 to avoid missing compose compiler artifacts.
        kotlinVersion = "1.9.24"
        androidXBrowser = "1.8.0"
    }
    repositories {
        google()
        mavenCentral()
        // For Kotlin Compose compiler artifacts required by Expo SDK 52 / expo-modules-core
        maven {
            url 'https://maven.pkg.jetbrains.space/public/p/compose/dev'
        }
        // Kotlin compiler dev artifacts (including compose compiler plugin)
        maven {
            url 'https://maven.pkg.jetbrains.space/kotlin/p/kotlin/dev'
        }
    }

    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        classpath("io.sentry:sentry-android-gradle-plugin:5.3.0")
    }
}

allprojects {
    repositories {
        maven {
            // expo-camera bundles a custom com.google.android:cameraview
            url "$rootDir/../node_modules/expo-camera/android/maven"
        }
        maven {
            url 'https://www.jitpack.io'
        }

        google()
        mavenCentral()
        // Kotlin Compose compiler artifacts
        maven {
            url 'https://maven.pkg.jetbrains.space/public/p/compose/dev'
        }
        maven {
            url 'https://maven.pkg.jetbrains.space/kotlin/p/kotlin/dev'
        }
    }
}

subprojects {
    afterEvaluate { project ->
        if (project.name == 'react-native-quick-crypto') {
            project.android.defaultConfig.minSdkVersion = 24
        }
    }
}

apply plugin: "com.facebook.react.rootproject"
