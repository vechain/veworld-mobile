import { StyleProp, ViewStyle } from "react-native"

export const HeaderStyle: StyleProp<ViewStyle> = {
    height: 48,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
}

export const HeaderStyleV2: StyleProp<ViewStyle> = {
    height: 64,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
}
