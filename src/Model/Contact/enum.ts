/**
 * To determine the type of contact
 *
 * @field KNOWN - Represents contacts added manually by the user
 * @field CACHE - Cached addresses from token transfers
 */
export enum ContactType {
    KNOWN = "known",
    CACHE = "cache",
}
