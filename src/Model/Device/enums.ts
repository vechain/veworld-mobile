export enum DEVICE_CREATION_ERRORS {
    INVALID_IMPORT_DATA = "Invalid import data",
    INCORRECT_PASSWORD = "Incorrect password",
    ADDRESS_EXISTS = "A device with this address already exists",
    UNKNOWN_ERROR = "Unknown error",
}
