export enum CURRENCY {
    USD = "USD",
    EUR = "EUR",
}

export enum CURRENCY_SYMBOLS {
    USD = "$",
    EUR = "€",
}

export enum SYMBOL_POSITIONS {
    BEFORE = "before",
    AFTER = "after",
}
